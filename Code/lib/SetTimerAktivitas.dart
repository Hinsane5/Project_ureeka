import 'package:flutter/material.dart';

class SetTimerAktivitas extends StatelessWidget {
  const SetTimerAktivitas({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Set Timer Aktivitas")),
      body: const Center(child: Text("Halaman Timer Aktivitas")),
    );
  }
}
